# NOTES Server TODO

1. 配布ZIPを notes サーバへ配置（`*.zip` と `*.zip.sha256`）。
2. LP用原稿を配置（`runbooks/QE_LP_COPY.md` をベースに反映）。
3. proofページへ証跡リンクを配置（`runbooks/QE_PROOF_PACK_INDEX.md`）。
4. `mkdocs.yml` に新規ページを追加。
5. `deploy.notes.sh` を実行して公開反映。
6. 公開後にリンク疎通と表示を確認。
7. 問題なければ広告導線を開始（段階投入）。
