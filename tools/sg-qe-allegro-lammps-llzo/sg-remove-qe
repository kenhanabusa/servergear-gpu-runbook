#!/usr/bin/env bash
set -euo pipefail
# normalized return codes
RC_OK=0
RC_SKIP=20
RC_FAIL=40

DIR="$(cd "$(dirname "$0")" && pwd)"
source "$DIR/bin/sg_runbook_lib.sh"
sg_begin "QE_REMOVE"

sg_init_logs
STEP="QE-REMOVE-090"
sg_skip "v0.1: remover is not implemented here." "$STEP" "E_NOT_IMPLEMENTED" "sg-remove-qe"
